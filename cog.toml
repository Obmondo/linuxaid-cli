tag_prefix = "v"
branch_whitelist = ["main"]
ignore_merge_commits = true
disable_changelog = true

post_bump_hooks = [
  # origin is the GitHub repo, gitea its mirror.
  "git push origin main {{version_tag}}",
  "git push gitea main {{version_tag}}",
]
